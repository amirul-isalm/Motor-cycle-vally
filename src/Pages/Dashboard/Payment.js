import { Typography } from '@mui/material';
import { Box } from '@mui/system';
import React from 'react';

const Payment = () => {
    return (
        <Box>

            <Typography variant="h2"> Payment system comeing soon...</Typography>
        </Box>
    );
};

export default Payment;